{
  "name": "code-playground-project",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node server.js",
    "client": "cd code-playground && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
